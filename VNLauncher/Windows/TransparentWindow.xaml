<Window x:Class="VNLauncher.Windows.TransparentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VNLauncher.Windows"
        mc:Ignorable="d"
        AllowsTransparency="True" Background="Transparent" WindowStyle="None" Topmost="True">
    <Canvas x:Name="mainCavas">
        <Canvas x:Name="gameWindowCavas">
            <Grid x:Name="gameWindowGrid">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="upRow"/>
                    <RowDefinition Name="captionRow"/>
                    <RowDefinition x:Name="downRow"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="leftColumn"/>
                    <ColumnDefinition x:Name="captionColumn"/>
                    <ColumnDefinition x:Name="rightColumn"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Row="1" Grid.Column="1" BorderBrush="Red" BorderThickness="2"/>
            </Grid>
        </Canvas>
    </Canvas>
</Window>
